# HoopTrace v0.1.0 发布执行记录

- 状态：本地签名候选版已通过，待远程 CI 与公开发布
- 版本：`0.1.0+1`
- 目标标签：`v0.1.0`
准备日期：2026-08-21

本文件记录首个公开版本的实际发布证据。通用流程见 `release-checklist.md`。

## 已完成

- [x] `pubspec.yaml`、应用内版本、CHANGELOG、Fastlane changelog 和发布说明一致。
- [x] Android Application ID 固定为 `io.github.x1a0y4ngren.hooptrace`，最低 API 为 24。
- [x] 已审计运行时依赖、Android 权限和隐私说明；应用无账号、广告、分析、追踪或联网后端。
- [x] 商店文案、图标和截图已准备；截图只含经过整理的演示数据，无个人信息。
- [x] 2026-08-21 执行 `dart format --output=none --set-exit-if-changed .`：通过。
- [x] 2026-08-21 执行 `flutter analyze`：通过，无问题。
- [x] 2026-08-21 执行 `flutter test`：通过，共 119 项测试。
- [x] 从上一正式版升级测试：不适用，`v0.1.0` 是首次公开发布。
- [x] 2026-08-21 创建永久 Android 发布密钥；密钥位于 Git 仓库外，别名与密码经 `keytool` 验证，本地签名配置已被 Git 忽略。
- [x] 使用兼容 SHA-256 实现生成仓库外校验记录，并确认密钥文件未被修改。
- [x] 维护者已确认永久 Android 发布密钥完成至少两份加密离线备份。
- [x] 发布脚本会在测试后清理旧插件注册文件，再由 Flutter Release 模式重建运行时插件列表，排除 `integration_test` 并保留正式插件。
- [x] 由干净提交生成签名 APK；签名、应用 ID、版本号、SDK、权限和 SHA-256 均通过发布脚本校验。
- [x] 在 Android 16 / API 36 模拟器覆盖安装确切候选 APK，完成冷启动、计分数据保留、复盘历史和横屏流程复验。
- [x] 完成 Android SAF 备份验收：目录授权、手动写入、开启自动备份、强制停止后冷启动自动写入均成功，无 `.partial` 残留。
- [x] 使用自动备份文件完成校验与原子恢复，历史记录保持红方 `3:2`、时长 `02:44`、落点完整度 `50%`，恢复后自动备份位置按设计重置。
- [x] 旧 `/storage/...` 自动备份配置会自动失效并关闭，不会继续显示为已开启；单元测试覆盖该迁移。

## 待完成

- [ ] 推送完整源码并确认 GitHub Actions 在候选提交上通过。
- [ ] 创建签名标签 `v0.1.0` 和 GitHub Release，上传 APK、`SHA256SUMS`、证书及包信息记录。
- [ ] 从公开 Release 重新下载 APK，复核 SHA-256 与签名证书指纹。

## 发布证据

- 候选应用代码提交：`14ce1dbefeba`
- GitHub Actions：待填写
- APK：`HoopTrace-v0.1.0-android.apk`
- APK SHA-256：`28952d5f611abea95818d2b046aff61b2b98a18207e60fe35a9906237e8da666`
- 签名：单一签名者，APK Signature Scheme v2
- 签名证书 DN：`CN=HoopTrace, OU=Open Source, O=HoopTrace, C=CN`
- 签名证书 SHA-256：`c07fd01660fd08437fa1232ede247ecc860226b5ccab78438ebfe5ddd5d64898`
- 包信息：`io.github.x1a0y4ngren.hooptrace`，`versionName=0.1.0`，`versionCode=1`，`minSdk=24`，`targetSdk=36`
- 权限：仅 Android 动态接收器内部权限；无 `INTERNET` 权限
- 设备与 Android 版本：`sdk_gphone64_x86_64` 模拟器，Android 16 / API 36
- GitHub Release：待填写
