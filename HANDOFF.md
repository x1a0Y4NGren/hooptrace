# HoopTrace 1.1 交接记录

更新日期：2026-09-14

## 当前状态

项目当前版本为 `1.1.0+3`。正式发布版本仍为 `v1.0.0`；本轮成果同步到 `main`，不创建新 tag 或 Release。核心计分、历史、复盘、分析和备份继续保持离线，无账号、广告、遥测或运营后端。

## 本轮已完成

- 计分侧栏固定为双方 `+1 / +2 / +3 / 未命中` 四行等距操作；犯规移动到顶部单一入口，并可选择蓝方或红方。
- 未命中支持球场优先、直接记录和 10 秒补点；其位置使用白色底、蓝红球队色叉号。
- “更多 → 投篮显示”可独立隐藏蓝方或红方未中点。开关仅作用于当前计分页，不删除数据，也不影响统计和回放。
- 统一撤回覆盖得分、后补位置、未中、犯规、罚球、球权、备注和自定义事件；暂停、继续与结束比赛不进入撤回栈。
- 首页 README 截图、功能描述、中英文文案、Widget/Golden/路由测试均已同步。

## 验证状态

- `dart format --output=none --set-exit-if-changed .`
- `flutter analyze`：无问题。
- `flutter test`：955 项全部通过。
- `integration_test/main_loop_test.dart`：Android 模拟器主流程通过。
- `flutter build apk --debug`：构建、安装和启动通过。

## 已知边界与踩坑

- 未中点显隐是页面表现状态，重新进入计分页默认恢复全部显示；不要把它持久化或用于过滤数据库、统计和回放。
- 撤回顺序以持久化审计顺序为准。得分后补位置需要先撤位置、再撤事件；球场优先的事件与位置则一次原子撤回。
- 隐藏位置必须同时从绘制和点击命中检测中排除，否则不可见标记会阻挡新的球场点击。
- Android 集成测试可能继承上一场计分页的横屏方向；主页操作在点击前必须 `ensureVisible`，仅等待 `.hitTestable()` 会因滚动视口裁剪而超时。
- Golden 更新前必须人工检查差异；README 应引用当前 canonical Golden，而不是遗留的 `*_linux.png` 截图。
- iOS 尚未完成真机验收；v1.1 的正式签名、tag、Release 和 F-Droid 流程仍需单独执行。
